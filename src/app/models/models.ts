export interface MMRChart {
  bracket: string;
  reward: string;
}

export interface ArenaMetrics {
  date: string;
  win: number;
  lose: number;
}

export interface SLPMetrics {
  date: string;
  slp: number;
}